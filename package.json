{
  "name": "nextvisit",
  "version": "0.1.0",
  "private": true,
  "description": "Next Visit — booking medical appointments in a single clinic",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter @nextvisit/server db:migrate",
    "db:seed": "pnpm --filter @nextvisit/server db:seed"
  },
  "packageManager": "pnpm@11.2.2"
}